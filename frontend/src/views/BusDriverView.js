import React from 'react';
import { Text, View, TextInput } from 'react-native'

const BusDriverView = () => {
    return (
        <View style={{padding: 10}}>
            <Text style={{padding: 10, fontSize: 40}}>Bus Driver View</Text>
        </View>
    );
}

export default BusDriverView;