import React from 'react'
import { Text, View, TouchableOpacity, Stylesheet } from 'react-native'

const ActivityChange = ({ studentName, date, permanent }) => {

    return (
        <View>

        </View>
    );
}

export default ActivityChange;